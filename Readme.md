# Simple implementation of Socket.io interface with Highcharts gauge on Typescript-Express-Handlebars.

## Installation and running:
* run npm install
* run dev on context with run npm dev
* buid with tsc